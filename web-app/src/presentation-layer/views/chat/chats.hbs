<div class="column">
      <div class="column col-11 col-sm-12">
        {{#if message}}
            <h3>{{message}}</h3>
        {{/if}}
        {{#if Chats.length}}
            {{#each Chats}}
                <div class="card">
                    <a href="/chat/{{this.dataValues.sender_id}}">{{this.dataValues.username}}</a>
                    <!-- mysql <a href="/chat/{{this.sender_id}}">{{this.username}}</a> -->
                </div>
            {{/each}}
        {{else}}
        <h1>No chat groups yet</h1>
        {{/if}}
    </div>
</div>