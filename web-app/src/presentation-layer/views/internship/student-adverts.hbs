<header class="navbar columns">
    <section class="navbar-section column col-2"></section>
    <section class="navbar-center column col-8">
        <form action="/search" method="GET" class="column">
        <div class="input-group input-inline column col-12">
            <select class="form-select column col-2" id="option" name="type">
                <option>student</option>
                <option>recruiter</option>
            </select>
            <input class="form-input" type="text" placeholder="search" name="q" value="{{search}}">
            <button class="btn btn-primary input-group-btn">Search</button>
        </div>
        </form>
    </section>
    <section class="navbar-section column col-2"></section>
</header>
<br>
<div class="columns col-xl">
    <div class="column col-3"></div>
    <div class="column col-6 col-md-12">
        <h3>View student adverts</h3>
        {{#if validationErrors.length}}
        <div class="toast toast-error">
            {{validationErrors}}
        </div>
        {{/if}}
        {{#if Posts.length}}
            {{#each Posts}}
                <div class="card">
                    <div class="card-header">
                        <div class="card-title h5"><a>{{this.dataValues.title}}</a></div>
                        <div class="card-title h6"><a href="/advert/{{this.dataValues.id}}?type=student">Advert</a></div>
                        {{#if ../deleteOrUpdate}}
                            <form action="/delete/advert" method="POST">
                                <input type="hidden" name="_csrf" value="{{../csrfToken}}">
                                <input type="hidden" name="id" value="{{this.dataValues.id}}">
                                <div class="form-group">
                                    <input class="form-submit" type="submit" placeholder="Delete" value="Delete">
                                </div>
                            </form>
                            <a href="/update/advert?type=student&id={{this.dataValues.id}}">Update</a>
                        {{/if}}
                        <div class="card-subtitle text-gray">
                            <a href="/send/chat/{{this.dataValues.posted_by}}">Send message</a>
                            <ul>
                                <li>{{this.dataValues.field}}</li>
                                <li>{{this.dataValues.contact}}</li>
                                <li>{{this.dataValues.start_date}}</li>
                                <li>{{this.dataValues.end_date}}</li>
                                <li>{{this.dataValues.posted_by}}</li>
                            </ul>
                        </div>
                    </div>
                    <div class="card-body" id="card-content">
                        {{{this.dataValues.body}}}
                    </div>
                    <!--
                        use this when mysql is the db

                        <div class="card-header">
                        <div class="card-title h5"><a>{{this.title}}</a></div>
                        <div class="card-subtitle text-gray">
                            <ul>
                                <li>{{this.field}}</li>
                                <li>{{this.contact}}</li>
                                <li>{{this.start_date}}</li>
                                <li>{{this.end_date}}</li>
                                <li>{{this.posted_by}}</li>
                            </ul>
                        </div>
                        </div>
                        <div class="card-body" id="card-content">
                            {{{this.body}}}
                        </div>
                    -->
                    <br>
                </div>
                <br>
            {{/each}}
        {{else}}
        <h1>No adverts yet</h1>
        {{/if}}
    </div>
    <div class="column col-3"></div>
</div>